/**
 * Mock for SkipToContent component
 */
import React from "react";

const SkipToContent = jest.fn(() => (
  <div data-testid="skip-to-content">Skip to content</div>
));

export default SkipToContent;
